Language resolution, aliases and caps.

```rust
fn main() {
    let greeting: &str = "hello";
    println!("{greeting}");
}
```

```ts
export const add = (a: number, b: number): number => a + b;
```

```sh
set -euo pipefail
echo "$HOME" | tr a-z A-Z
```

```yml
name: build
on: [push]
```

```c++
template <typename T> T max(T a, T b) { return a > b ? a : b; }
```

```wingdings
this language does not exist
```

```diff
--- a/x
+++ b/x
-removed
+added
 kept
```
